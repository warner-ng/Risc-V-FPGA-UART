# RISC-V UART 串口模拟测试

## 测试目标

在 VSCode 中使用 Icarus Verilog 进行本地仿真，模拟 UART 串口通信，测试 RISC-V 处理器动态加载和执行机器码的功能。

## 测试指令

本次测试将通过 UART 串口发送以下 4 条机器码指令：

| 指令 | 机器码 | 说明 | 预期结果 |
|------|--------|------|---------|
| `addi x10, x0, 100` | `0x06400513` | x10 = 0 + 100 | x10 = 100 |
| `addi x11, x0, 200` | `0x0C800593` | x11 = 0 + 200 | x11 = 200 |
| `add x12, x10, x11` | `0x00B50633` | x12 = x10 + x11 | x12 = 300 |
| `nop` | `0x00000013` | 结束标记 | 停止执行 |

**最终结果**: x12 寄存器应该包含 300 (0x0000012C)

## 文件说明

- **riscvsingle_dynamic_load.v**: 主设计文件
  - `fpga_top_dynamic`: 顶层模块，包含 UART 控制器和状态机
  - `top_dynamic`: RISC-V CPU 核心包装
  - `uart_tx_simple` / `uart_rx_simple`: UART 收发器

- **riscv_dynamic_tb.v**: 测试平台
  - 模拟串口发送/接收
  - 自动发送测试指令
  - 验证执行结果
  - 生成详细的调试信息

- **run_sim.bat**: 仿真运行脚本
  - 自动编译 Verilog 文件
  - 运行仿真
  - 生成波形文件

## 运行步骤

### 1. 在 VSCode 终端中运行仿真

```bash
.\run_sim.bat
```

或者手动运行：

```bash
# 编译（加速仿真模式）
iverilog -DSIM_FAST -o riscv_dynamic_sim.vvp riscvsingle_dynamic_load.v riscv_dynamic_tb.v

# 运行仿真
vvp riscv_dynamic_sim.vvp
```

### 2. 查看仿真输出

仿真将在控制台输出详细信息，包括：

- **UART 通信**: 显示发送和接收的每个字节
- **状态机转换**: 显示 FPGA 状态机的状态变化
- **指令加载**: 显示写入指令内存的内容
- **CPU 执行**: 显示每条指令执行时的 PC、指令码和寄存器值
- **最终结果**: 显示 x12 寄存器的值并验证是否正确

### 3. 查看波形

仿真完成后会生成 `riscv_dynamic_test.vcd` 波形文件。

如果安装了 GTKWave，可以使用以下命令查看：

```bash
gtkwave riscv_dynamic_test.vcd
```

## 预期输出示例

```
========================================
  RISC-V Dynamic Load Simulation Test
========================================

[170] Reset released

--- Waiting for RISC-V startup ---
[STATE@220] -> INIT
[TX -> RISC-V: 0x52]  # 'R'
[TX -> RISC-V: 0x56]  # 'V'
[TX -> RISC-V: 0x0A]  # '\n'
[STATE@450] -> IDLE
RISC-V is ready!

--- Sending machine code (4 instructions) ---
[TX -> RISC-V: 0x10]  # CMD_LOAD_CODE
[TX -> RISC-V: 0x10]  # Length = 16 bytes
[TX -> RISC-V: 0x00]

  Instr 1: addi x10, x0, 100  ->  0x06400513
[IMEM_WR@1250] Addr=0x00 Data=0x06400513

  Instr 2: addi x11, x0, 200  ->  0x0C800593
[IMEM_WR@1550] Addr=0x01 Data=0x0C800593

  Instr 3: add  x12, x10, x11  ->  0x00B50633
[IMEM_WR@1850] Addr=0x02 Data=0x00B50633

  Instr 4: nop (end marker)  ->  0x00000013
[IMEM_WR@2150] Addr=0x03 Data=0x00000013

[RX <- RISC-V: 0x06]  # ACK
Received ACK - Load success!

--- Executing program ---
[STATE@2300] -> RUN (执行开始)
[CPU@2310] PC=0x00000000 Instr=0x06400513 | x10=100 x11=0 x12=0
[CPU@2320] PC=0x00000004 Instr=0x0C800593 | x10=100 x11=200 x12=0
[CPU@2330] PC=0x00000008 Instr=0x00B50633 | x10=100 x11=200 x12=300
[CPU@2340] PC=0x0000000C Instr=0x00000013 | x10=100 x11=200 x12=300

[RX <- RISC-V: 0x0F]  # DONE
Received DONE - Execution complete!

--- Getting result ---
[RX <- RISC-V: 0x2C]  # Result byte 0
[RX <- RISC-V: 0x01]  # Result byte 1
[RX <- RISC-V: 0x00]  # Result byte 2
[RX <- RISC-V: 0x00]  # Result byte 3

========================================
  TEST RESULT
========================================
  x12 = 300 (0x0000012C)
  Expected: 300 (100 + 200)

  *** TEST PASSED! ***
========================================
```

## UART 通信协议

### 命令格式

1. **加载代码** (CMD_LOAD_CODE = 0x10)
   ```
   发送: 0x10 [长度低字节] [长度高字节] [机器码字节...]
   接收: 0x06 (ACK)
   ```

2. **运行代码** (CMD_RUN = 0x20)
   ```
   发送: 0x20
   接收: 0x0F (DONE)
   ```

3. **获取结果** (CMD_GET_RESULT = 0x30)
   ```
   发送: 0x30
   接收: [结果 4 字节，小端序]
   ```

### 数据格式

- 所有多字节数据使用**小端序** (LSB first)
- 机器码按字节传输，每 4 字节组成一条 32 位指令
- 结果从 x12 寄存器读取

## 调试技巧

1. **查看状态机**: 观察 `[STATE]` 标记，了解 FPGA 状态转换
2. **查看指令加载**: 观察 `[IMEM_WR]` 标记，确认指令正确写入
3. **查看 CPU 执行**: 观察 `[CPU]` 标记，查看每条指令的执行情况
4. **查看 UART 通信**: 观察 `[TX]` 和 `[RX]` 标记，确认通信正常
5. **使用波形查看器**: 用 GTKWave 打开 .vcd 文件，详细分析信号时序

## 故障排查

### 1. 编译错误
- 确保 Icarus Verilog 已正确安装
- 检查文件路径是否正确
- 确认所有模块定义完整

### 2. 结果不正确
- 检查机器码字节序是否正确（小端序）
- 查看 CPU 执行日志，确认寄存器值
- 使用 GTKWave 查看波形，分析信号

### 3. 仿真超时
- 检查 UART 波特率分频值（仿真中应使用较小值）
- 确认定义了 `-DSIM_FAST` 编译选项

## 扩展测试

你可以修改 [riscv_dynamic_tb.v](riscv_dynamic_tb.v) 中的指令序列来测试其他功能：

```verilog
// 例如：测试减法
// sub x12, x11, x10  ->  0x40A58633
uart_send_byte(8'h33);
uart_send_byte(8'h86);
uart_send_byte(8'hA5);
uart_send_byte(8'h40);
```

## 参考资料

- RISC-V 指令集手册: https://riscv.org/specifications/
- UART 协议说明: [README_UART.md](README_UART.md)
- Icarus Verilog 文档: http://iverilog.icarus.com/
