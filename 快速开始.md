# 快速开始 - RISC-V UART 串口仿真

## 一键运行

在 VSCode 终端中执行：

```bash
.\run_sim.bat
```

或者分步执行：

```bash
# 1. 编译
iverilog -DSIM_FAST -o riscv_dynamic_sim.vvp riscvsingle_dynamic_load.v riscv_dynamic_tb.v

# 2. 运行
vvp riscv_dynamic_sim.vvp

# 3. 查看波形（可选）
gtkwave riscv_dynamic_test.vcd
```

## 测试内容

本仿真测试通过 UART 串口发送以下机器码：

```
指令 1: addi x10, x0, 100  ->  机器码: 0x06400513  (x10 = 100)
指令 2: addi x11, x0, 200  ->  机器码: 0x0C800593  (x11 = 200)
指令 3: add  x12, x10, x11  ->  机器码: 0x00B50633  (x12 = 300)
指令 4: nop                 ->  机器码: 0x00000013  (结束标记)
```

**预期结果**: x12 = 300 ✅

## 仿真输出解读

### 1. 启动阶段
```
[995000] Reset released
--- Waiting for RISC-V startup ---
[2065000] RX <- RISC-V: 0x52 ('R')    # RISC-V 发送 'R'
[3085000] RX <- RISC-V: 0x56 ('V')    # RISC-V 发送 'V'
[4105000] RX <- RISC-V: 0x0a ('\n')   # RISC-V 发送 '\n'
RISC-V is ready!                       # RISC-V 就绪
```

### 2. 加载代码阶段
```
[4105000] TX -> RISC-V: 0x10           # 发送 LOAD_CODE 命令
[5155000] TX -> RISC-V: 0x10           # 代码长度 = 16 字节
[6205000] TX -> RISC-V: 0x00

[IMEM_WR@11405000] Addr=0x00 Data=0x06400513  # 指令 1 写入内存
[IMEM_WR@15605000] Addr=0x01 Data=0x0c800593  # 指令 2 写入内存
[IMEM_WR@19805000] Addr=0x02 Data=0x00b50633  # 指令 3 写入内存
[IMEM_WR@24005000] Addr=0x03 Data=0x00000013  # 指令 4 写入内存

[25055000] RX <- RISC-V: 0x06          # RISC-V 返回 ACK
Received ACK - Load success!           # 加载成功
```

### 3. 执行阶段
```
[25055000] TX -> RISC-V: 0x20          # 发送 RUN 命令

# CPU 开始执行指令，每行显示：PC、指令、寄存器值
[CPU@26055000] PC=0x00000000 Instr=0x06400513 | x10=0   x11=0   x12=0
[CPU@26065000] PC=0x00000004 Instr=0x0c800593 | x10=100 x11=0   x12=0
[CPU@26075000] PC=0x00000008 Instr=0x00b50633 | x10=100 x11=200 x12=0
[CPU@26085000] PC=0x0000000c Instr=0x00000013 | x10=100 x11=200 x12=300

[27155000] RX <- RISC-V: 0x0f          # RISC-V 返回 DONE
Received DONE - Execution complete!    # 执行完成
```

### 4. 获取结果阶段
```
[27155000] TX -> RISC-V: 0x30          # 发送 GET_RESULT 命令
[29215000] RX <- RISC-V: 0x2c          # 结果字节 0 (LSB)
[30235000] RX <- RISC-V: 0x01          # 结果字节 1
[31255000] RX <- RISC-V: 0x00          # 结果字节 2
[32275000] RX <- RISC-V: 0x00          # 结果字节 3 (MSB)

========================================
  TEST RESULT
========================================
  x12 = 300 (0x0000012c)               # 结果正确！
  Expected: 300 (100 + 200)

  *** TEST PASSED! ***                 # 测试通过
========================================
```

## 关键观察点

### ✅ 串口通信正常
- TX/RX 交互正确
- 协议握手成功（ACK、DONE）

### ✅ 指令加载正确
- 4 条指令正确写入内存
- 地址从 0x00 到 0x03

### ✅ CPU 执行正确
- PC 按序递增（0x00 → 0x04 → 0x08 → 0x0C）
- 寄存器值按预期变化：
  - x10: 0 → 100
  - x11: 0 → 200
  - x12: 0 → 300

### ✅ 结果返回正确
- x12 = 300 (0x0000012C)
- 小端序传输：0x2C 0x01 0x00 0x00

## 修改测试

要测试其他指令，修改 [riscv_dynamic_tb.v](riscv_dynamic_tb.v) 的第 140-170 行：

```verilog
// 例如：测试减法 sub x12, x11, x10
uart_send_byte(8'h33);  // 指令字节 0
uart_send_byte(8'h86);  // 指令字节 1
uart_send_byte(8'hA5);  // 指令字节 2
uart_send_byte(8'h40);  // 指令字节 3 (MSB)
```

## 文件说明

- **riscvsingle_dynamic_load.v**: 主设计（FPGA 逻辑）
- **riscv_dynamic_tb.v**: 测试平台（仿真环境）
- **run_sim.bat**: 编译和运行脚本
- **riscv_dynamic_test.vcd**: 波形文件（仿真后生成）

## 更多信息

详细说明请参考：[仿真测试说明.md](仿真测试说明.md)
